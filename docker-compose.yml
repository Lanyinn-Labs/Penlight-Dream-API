services:
  penlight-dream-api:
    build: .
    image: penlight-dream-api:latest
    restart: unless-stopped
    env_file:
      - .env
    environment:
      HOST: 0.0.0.0
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
